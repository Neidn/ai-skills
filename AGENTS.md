# 작업 규칙

이 파일은 항상 적용되는 기본 규칙이다. 특정 작업에만 필요한 절차는 `skills/` 아래의
SKILL.md로 분리한다. Claude Code는 `CLAUDE.md`, Gemini CLI는 `GEMINI.md` 심볼릭 링크로
같은 파일을 읽는다.

## 착수 전

- 작업이 3단계 이상이면 먼저 계획을 제시하고 승인을 받는다. 계획은 **목표 / 단계 /
  건드릴 파일 / 확인 방법** 네 항목으로 짧게 쓴다.
- 요구사항이 모호하면 추측해서 진행하지 말고 묻는다. 단, 질문은 한 번에 모아서 한다.
- 이미 있는 코드·설정을 먼저 읽고 기존 패턴을 따른다. 새 패턴을 도입할 때는 이유를 말한다.

## 진행 중

- 같은 명령이나 수정을 **3회 반복해도 해결되지 않으면 멈추고** 무엇을 시도했고 무엇이
  막혔는지 보고한다. 변형을 계속 시도하지 않는다.
- 반복 작업에는 최대 반복 횟수와 종료 조건을 먼저 정한다.
- 파일 전체를 다시 쓰지 말고 바뀌는 부분만 수정한다.
- 요청하지 않은 리팩터링, 포맷팅, 주석 정리를 끼워 넣지 않는다.

## 보고

- 결론을 먼저, 근거를 뒤에 쓴다.
- **추측한 내용과 확인한 내용을 구분해서 표시한다.** 이게 가장 중요하다.
- 명령 출력을 그대로 붙여넣지 말고 판단에 필요한 부분만 인용한다.
- 오류를 발견하면 요청 범위 밖이어도 짧게 알린다. 고치는 것은 별개로 확인받는다.

## 인프라 작업

- 운영 환경에 영향을 주는 명령은 실행 전에 무엇이 바뀌는지 먼저 설명한다.
- 삭제·재기동·보안 정책 변경은 항상 확인을 받는다.
- 자격증명, 계정 키, 실제 공인 IP를 파일이나 로그에 남기지 않는다.
- 변경 작업에는 롤백 방법을 같이 적는다.
